{{- if (eq .Values.installationMode "OEA-AP") -}}
apiVersion: v1
stringData:
  audit-local.yml: |
    {{- tpl (.Files.Get "config/oea-audit/audit-local.yaml") . | nindent 4 }}
kind: Secret
metadata:
  labels:
    app: oea
    component: auditservice
{{ include "oea.standard-labels" . | indent 4 }}
  name: oea-audit-service-config
{{- end }}
