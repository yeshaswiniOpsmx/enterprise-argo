{{- if (eq .Values.installationMode "OEA-AP") -}}
apiVersion: v1
stringData:
  visibility-local.yml: |
    {{- tpl (.Files.Get "config/oea-visibility/visibility-local.yml") . | nindent 4 }}
kind: Secret
metadata:
  name: oea-visibility-config
  labels:
    app: oea
    component: visibility
{{ include "oea.standard-labels" . | indent 4 }}
{{- end -}}
