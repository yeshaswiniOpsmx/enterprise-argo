{{- if (eq .Values.installationMode "OEA-AP") -}}
apiVersion: v1
data:
  dashboard-local.yml: |
    {{- tpl (.Files.Get "config/oea-dashboard/dashboard-local.yml") . | nindent 4 }}
kind: ConfigMap
metadata:
  name: oea-dashboard-config
  labels:
    app: oea
    component: dashboard
{{ include "oea.standard-labels" . | indent 4 }}
{{- end -}}
